<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sayanth10.englishthroughtamil">

    <application
        android:allowBackup="true"
        android:icon="@drawable/engforkids"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NumbersActivity"
            android:label="@string/numbers"
            android:theme="@style/numbersTheme" />
        <activity
            android:name=".ColorsActivity"
            android:label="@string/colors"
            android:theme="@style/colorsTheme" />
        <activity
            android:name=".FamilyActivity"
            android:label="@string/family_members"
            android:theme="@style/familyTheme" />
        <activity
            android:name=".PhrasesActivity"
            android:label="@string/phrases"
            android:theme="@style/phrasesTheme" />
        <activity android:name=".AnimalsActivity"
            android:label="@string/animals"
            android:theme="@style/AnimalsTheme"/>
        <activity android:name=".BirdsActivity"
            android:label="@string/birds"
            android:theme="@style/BirdsTheme"/>
        <activity android:name=".FruitsActivity"
            android:label="@string/fruits"
            android:theme="@style/FruitsTheme"/>
        <activity android:name=".VegetablesActivity"
            android:label="@string/vegetables"
            android:theme="@style/VegetableTheme"/>
        <activity android:name=".BodyPartsActivity"
            android:label="@string/body_parts"
            android:theme="@style/BodyPartsTheme"/>
    </application>

</manifest>